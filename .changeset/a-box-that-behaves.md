---
"@eliya-oss/agent-diff": patch
---

The box a comment is written in behaves like a text box: the caret is the cell it stands on rather than a character shifting the words, the arrows move a line at a time through the wrap, and `option` and `cmd` with them move by word and to the ends. `shift` with an arrow grows the selection to comment on.
