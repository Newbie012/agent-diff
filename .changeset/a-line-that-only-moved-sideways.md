---
"@eliya-oss/agent-diff": patch
---

feat(diff): a removed line and an added line that differ only in their indentation are washed dimmer than a change, so a block wrapped in a new function reads as one wrapper and not as a rewrite of every line inside it.
