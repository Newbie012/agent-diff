## 0.1.0-alpha.163

### Patch Changes

- fix(repo): `pnpm check-body` refuses a pull request body that says who reported a change, carries a private repository or ticket, or runs past 300 characters of prose, and `pnpm lint` holds every change intent, wiki page, PRD, the changelog and the README to the same rules.
