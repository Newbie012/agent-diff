## 0.1.0-alpha.161

### Patch Changes

- fix(diff): `e` opens a line inside its project — an editor that takes a folder is handed the branch's worktree with the file, and every editor is started in that worktree — and a command you type yourself is offered back as the command to keep.
