<!--
The format lives in .claude/skills/release-notes/SKILL.md — read it before filling this in.
Generate the `## What changed` block with `pnpm pr-summary`; never type it by hand.
Nothing private, ever: this repository and every attachment on it are public.
-->

One paragraph. What this changes, and what prompted it.

## What changed

<!-- pnpm pr-summary — paste its output here. Delete this section if the branch adds no change intent. -->

## <the symptom, not the fix>

The repro, as keys to press. Then what the fix was.

<!-- A screen that changed owes a before and an after:
     pnpm shot --keys "enter text:l" --label "<what it is>" --against origin/main -->
