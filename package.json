{
  "name": "adiff",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@effect/vitest": "^4.0.0-beta.102",
    "@kitlangton/terminal-control": "^0.6.0",
    "@types/node": "^26.1.2",
    "oxlint": "^1.76.0",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "effect": "^4.0.0-beta.102"
  },
  "scripts": {
    "check": "npm run typecheck && npm run lint && npm test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint && node scripts/check-style.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node src/main.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
